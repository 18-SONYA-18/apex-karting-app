
# Фильтры

**ID:** BS-001  
**Тип:** Bottom Sheet  
**Домен:** 01. Просмотр и фильтрация слотов  
**Приоритет:** High  
**Статус:** Черновик  
**Функциональные блоки:** FB-001-001 (Фильтр по дате/периоду), FB-001-002 (Фильтр по типу конфигурации трассы), FB-001-003 (Фильтр по наличию мест), FB-001-004 (Фильтр по маршалу)  
**Зона авторизации:** АЗ  
**Дизайн-макет:** [Figma — Фильтры / Filters Container (71:14300)](https://www.figma.com/design/ySEt0cjmRqmhdWyDlTpDM5/Апекс-приложение?node-id=71:14300)

> **Пресеты дат по дизайну** ([RR-D08](../3-design-brief/design-review.md)): быстрые чипы
> «Сегодня / Эта неделя / Выходные» + произвольный диапазон «С / По». Клиент маппит выбранный
> пресет в `date_from` / `date_to`. Остальные фильтры (тип конфигурации трассы, маршал из справочника,
> «только со свободными местами», «Применить» / «Сбросить») соответствуют макету.

---

## Содержание

- [История изменений](#история-изменений)
- [Обзор](#обзор)
- [Навигация](#навигация)
- [Входные данные](#входные-данные)
- [Применяемые логики](#применяемые-логики)
- [Свойства Bottom Sheet](#свойства-bottom-sheet)
- [Инициализация](#инициализация)
- [Используемые запросы](#используемые-запросы)
- [Макет экрана](#макет-экрана)
- [Элементы экрана](#элементы-экрана)
- [Состояния экрана](#состояния-экрана)
- [Действия пользователя](#действия-пользователя)
- [Связанные требования](#связанные-требования)
- [Критерии приёмки](#критерии-приёмки)
---

## История изменений

| Релиз | ТЗ | Описание изменений |
|-------|-----|-------------------|
| 0.1.0 | [BS-001 «Фильтры»](BS-001-filters.md) | Первичная версия ТЗ на шторку фильтров слотов. |

---

## Обзор

Bottom Sheet **BS-001 «Фильтры»** — форма выбора параметров фильтрации списка слотов. Открывается с экрана [SCR-002 «Список слотов»](SCR-002-slot-list.md) поверх него и позволяет клиенту сузить каталог предстоящих заездов до релевантных вариантов: по дате/периоду старта, типу конфигурации трассы, наличию свободных мест и маршалу.

Шторка **не выполняет запрос для отображения результата** — она лишь собирает комбинацию условий. Итоговая фильтрованная выдача (включая пустой результат) рендерится на SCR-002 после нажатия «Применить». BS-001 — это форма выбора параметров, а не отдельный экран результатов.

Контекст использования — трасса, солнце, спешка: выбор должен быть крупным, быстрым и выполнимым одним пальцем (см. [foundations §1](../3-design-brief/00-foundations.md)).

### User Story

> Как клиент, я хочу фильтровать список слотов по дате/периоду, типу конфигурации трассы, наличию свободных мест и маршалу,
> чтобы быстро находить подходящий вариант и сокращать путь к записи.

### Бизнес-ценность

- Короткий путь к записи (NFR-2): клиент быстрее доходит от каталога до брони.
- Снижение оттока: не нужно листать весь список — релевантные слоты находятся за минимум тапов.
- Релевантность выдачи: гибкая комбинация условий (AND между группами, OR внутри группы) под индивидуальный запрос.

---

## Навигация

### Входящая (откуда открывается)

| Источник | Триггер | Условие | Передаваемые параметры |
|----------|---------|---------|------------------------|
| [SCR-002 «Список слотов»](SCR-002-slot-list.md) | Тап по иконке/кнопке «Фильтры» | Всегда (клиент в АЗ) | `current_filters` (текущие применённые значения фильтров SCR-002) |

### Исходящая (куда ведёт)

| Назначение | Триггер | Передаваемые параметры |
|------------|---------|------------------------|
| [SCR-002 «Список слотов»](SCR-002-slot-list.md) | Тап «Применить» | `date_from`, `date_to`, `track_type[]`, `marshal_id`, `only_available` (выбранные значения) |
| [SCR-002 «Список слотов»](SCR-002-slot-list.md) | Закрытие (swipe-to-close / тап по бэкдропу / кнопка закрытия) | — (применённые ранее фильтры не изменяются) |

---

## Входные данные

| Название | Тип | Возможные значения | Описание |
|----------|-----|-------------------|----------|
| `current_filters` | Состояние (от SCR-002) | Объект с полями `date_from`, `date_to`, `track_type[]`, `marshal_id`, `only_available` | Текущие применённые на SCR-002 фильтры. Используются для предзаполнения контролов при открытии шторки, чтобы клиент видел и редактировал актуальный выбор. По умолчанию (фильтры не задавались) — все поля пустые, `only_available = false`. |

---

## Применяемые логики

| Логика | Элемент/Триггер | Описание |
|--------|-----------------|----------|
| [LOGIC-005 Фильтрация слотов](09_Логики/LOGIC-005_Фильтрация-слотов.md) | Кнопка «Применить» | Сборка параметров запроса `listSlots` из выбранных значений: OR внутри группы (мультивыбор `track_type[]`, `marshal_id`), AND между группами. Применяется на SCR-002. |

---

## Свойства Bottom Sheet

| Свойство | Значение |
|----------|----------|
| Высота | Динамическая (по контенту, не выше ~90% экрана; длинный контент скроллится внутри) |
| Закрытие свайпом | Да (жест вниз) |
| Закрытие по тапу вне области | Да (тап по бэкдропу) |
| Затемнение фона | Да (бэкдроп) |
| Кнопка закрытия | Да (явная кнопка/иконка закрытия в хедере шторки) |

> Дополнительно: видимый грабер (полоска) сверху; плавная анимация открытия/закрытия снизу вверх; таб-бар скрыт на время отображения шторки. Закрытие любым из способов (свайп / тап по бэкдропу / кнопка закрытия) — **без применения**: ранее применённые на SCR-002 фильтры не изменяются. См. [foundations §4.3](../3-design-brief/00-foundations.md).

---

## Инициализация

> **Примечание:** При открытии шторки контролы предзаполняются из входного `current_filters` (без запроса). Единственный запрос при открытии — справочник маршалов (`listMarshals`); он **неблокирующий**: остальные группы фильтров доступны сразу, поле маршала показывает состояние загрузки до получения справочника.

### Диаграмма загрузки

```mermaid
flowchart LR
    Start([onOpen]) --> Prefill[Предзаполнить контролы из current_filters]
    Start --> LoadInstr[listMarshals]

    Prefill --> Ready([Шторка интерактивна])

    LoadInstr --> Check{Ответ справочника?}
    Check -->|200 + items| InstrReady[Чипы маршалов]
    Check -->|200 + empty| InstrEmpty[Скрыть группу маршала]
    Check -->|Ошибка/сеть| InstrError[Неблокирующая ошибка + «Обновить»]

    InstrReady --> Ready
    InstrEmpty --> Ready
    InstrError --> Ready
```

### Запросы при открытии

| № | Запрос | Критичный | Зависит от | Условие |
|---|--------|-----------|------------|---------|
| 1 | [listMarshals](#listmarshals) | Нет | — | Всегда при открытии шторки |

> Заполнение остальных групп (дата, тип конфигурации трассы, переключатель мест) выполняется из входных данных `current_filters` без запросов. Полное описание запросов см. в секции [Используемые запросы](#используемые-запросы).

---

## Используемые запросы

> Все API-запросы экрана с полным описанием параметров и обработки ответов.

### listMarshals

**Тип:** REST  
**Метод:** GET  
**Спецификация:** [../api/marshals/api.yaml](../api/marshals/api.yaml) → `listMarshals`

**Триггер:** Инициализация (открытие шторки)

**Параметры:**

| Параметр | Тип | Обязательность | Источник | Описание |
|----------|-----|----------------|----------|----------|
| `limit` | integer | Нет | Клиент (default 20, max 100) | Размер страницы справочника маршалов. |
| `offset` | integer | Нет | Клиент (default 0) | Смещение для подгрузки следующих страниц справочника. |

**Обработка ответа:**

| Результат | Условие | UI-реакция |
|-----------|---------|------------|
| Загрузка | — | Скелетон/индикатор в группе «Маршал»; остальные группы интерактивны (неблокирующее) |
| Успех | `items` не пуст | Отрисовать чипы маршалов (`item.name`, ключ — `item.id`); предвыбрать те, чьи `id` есть в `current_filters.marshal_id` |
| Успех | `items` пуст | Скрыть группу «Маршал» (фильтровать не по чему); прочие фильтры доступны |
| HTTP 401 | — | Неблокирующая ошибка в группе «Маршал» (см. формат ниже); обработка истёкшего токена — на уровне приложения. Прочие фильтры работают |
| HTTP 5xx / default | — | Неблокирующая ошибка в группе «Маршал»: подпись «Не удалось загрузить маршалов» + «Обновить» (формат ниже); прочие фильтры работают |
| Сеть | Нет соединения | Неблокирующая ошибка в группе «Маршал»: подпись «Не удалось загрузить маршалов» + «Обновить» (формат ниже); прочие фильтры работают |

**Текст ошибки и формат подачи (неблокирующая ошибка справочника):**

- Ошибка **локализована в секции фильтра по маршалу** и **не блокирует** шторку: группа «Маршал» становится недоступной (чипы не отрисовываются), остальные группы (дата, тип конфигурации трассы, переключатель мест) и кнопка «Применить» работают как обычно.
- Подача — **ненавязчивая, внутри группы** (inline-подпись, а не модальная ошибка и не Error-заглушка всего экрана): короткая подпись **«Не удалось загрузить маршалов»** + инлайн-действие **«Обновить»** ([00-foundations §6](../3-design-brief/00-foundations.md) — кнопка повтора; см. также [LOGIC-008 Шаг 6](09_Логики/LOGIC-008_Паттерн-состояний-экрана.md)).
- Тап «Обновить» → повторный запрос `listMarshals` (группа снова в состояние загрузки/скелетон).
- **Снек об ошибке справочника не показывается**: это не провал действия и не pull-to-refresh, а неблокирующая частичная загрузка — обратная связь даётся подписью в самой секции. Снека успеха применения фильтров тоже нет — результат виден в списке на [SCR-002](SCR-002-slot-list.md) ([00-foundations §6.1](../3-design-brief/00-foundations.md)).

---

### listSlots

**Тип:** REST  
**Метод:** GET  
**Спецификация:** [../api/slots/api.yaml](../api/slots/api.yaml) → `listSlots`

**Триггер:** Тап «Применить» — запрос выполняется **на [SCR-002](SCR-002-slot-list.md)** (шторка к этому моменту уже закрыта). Здесь приведён только для трассировки query-параметров, которые шторка собирает и передаёт.

**Параметры (собираются шторкой из выбранных значений):**

| Параметр | Тип | Обязательность | Источник | Описание |
|----------|-----|----------------|----------|----------|
| `date_from` | string (date-time) | Нет | Группа «Дата старта» (пресет или поле «С») | Слоты со стартом не раньше указанного момента. |
| `date_to` | string (date-time) | Нет | Группа «Дата старта» (пресет или поле «По») | Слоты со стартом не позже указанного момента. |
| `track_type` | array[string] enum `novice` / `experienced` | Нет | Группа «Тип конфигурации трассы» (мультивыбор) | Множественный выбор; передаётся как повторяющийся параметр (`style: form, explode: true`). OR внутри группы. |
| `marshal_id` | array[string uuid] | Нет | Группа «Маршал» (мультивыбор) | Идентификаторы выбранных маршалов; повторяющийся параметр. OR внутри группы. |
| `only_available` | boolean (default false) | Нет | Переключатель «Только со свободными местами» | `true` — только слоты с `free_seats > 0`; `false` (default) — показываются все, заполненные с пометкой «Мест нет». |
| `limit` | integer | Нет | SCR-002 (default 20) | Пагинация выдачи (управляется SCR-002). |
| `offset` | integer | Нет | SCR-002 (default 0) | Пагинация выдачи (управляется SCR-002). |

**Обработка ответа:** выполняется на [SCR-002](SCR-002-slot-list.md) по [сквозному паттерну состояний](../3-design-brief/00-foundations.md): Loading → Content / Empty / Error. Пустой результат (UC-3 E1) — empty state с текстом «Нет слотов по условиям. Попробуйте изменить фильтры.» и действием изменить/сбросить фильтры.

---

## Макет экрана

### Структура

```
┌──────────────────────────────────────┐
│                ▭ грабер           [✕] │  ← swipe-to-close + кнопка закрытия
├──────────────────────────────────────┤
│  Фильтры                   Сбросить ▷ │  ← хедер: заголовок + сброс
├──────────────────────────────────────┤
│  Дата старта                          │  ← группа 1 (пресеты + диапазон)
│  [Сегодня] [Эта неделя] [Выходные]    │     быстрые пресеты (один)
│  ┌──────────────┐  ┌──────────────┐   │
│  │ С: 15 июня   │  │ По: 22 июня  │   │     произвольный диапазон
│  └──────────────┘  └──────────────┘   │
│                                        │
│  Тип конфигурации трассы               │  ← группа 2 (чипы, мультивыбор)
│  [✓ Новичковая]  [ Опытная ]           │
│                                        │
│  ┌──────────────────────────────────┐ │  ← группа 3 (toggle, выкл по умолч.)
│  │ Только со свободными местами  [ ○]│ │
│  └──────────────────────────────────┘ │
│                                        │
│  Маршал                                │  ← группа 4 (чипы, мультивыбор)
│  [✓ Анна] [ Игорь ] [ Мария ] …       │
│         (скролл при нехватке высоты)   │
├──────────────────────────────────────┤
│ ┌────────────────────────────────────┐│  ← фикс. нижний CTA
│ │            Применить               ││
│ └────────────────────────────────────┘│
└──────────────────────────────────────┘
            ░░░ бэкдроп (затемнение) ░░░
```

### Компоненты

| Компонент | Описание | Обязательность |
|-----------|----------|----------------|
| Грабер | Полоска сверху, индикатор swipe-to-close | Да |
| Кнопка закрытия | Явная иконка закрытия в хедере | Да |
| Заголовок «Фильтры» | Заголовок шторки | Да |
| Действие «Сбросить» | В хедере; активно при отличии от дефолта | Да |
| Группа «Дата старта» | Пресеты + произвольный диапазон (поля «С»/«По») | Да |
| Группа «Тип конфигурации трассы» | Чипы мультивыбора (Новичковая / Опытная) | Да |
| Переключатель «Только со свободными местами» | Toggle, default OFF | Да |
| Группа «Маршал» | Чипы мультивыбора из справочника `listMarshals` | Опционально (скрыта при пустом справочнике) |
| Кнопка «Применить» | Фиксированный нижний CTA во всю ширину | Да |

---

## Элементы экрана

> **Примечания:**
> - Колонка «Валидация»: для полей дат указано правило и поведение; для остальных — «—».
> - Логика после таблицы блока. Состояния выбора (чип, toggle) дублируются не только цветом, но формой/иконкой/текстом ([foundations §3.2](../3-design-brief/00-foundations.md)).

### 1. Хедер

| Элемент | Описание | Источник данных | Валидация | Действие |
|---------|----------|-----------------|-----------|----------|
| Грабер | Полоска для swipe-to-close | — | — | Свайп вниз → закрыть без применения |
| Заголовок «Фильтры» | Название шторки | — (статичный текст) | — | — |
| Кнопка закрытия «✕» | Явное закрытие шторки | — | — | Закрыть без применения → [SCR-002](SCR-002-slot-list.md) |
| Действие «Сбросить» | Возврат всех контролов к дефолту внутри шторки | — | — | Сброс к значениям по умолчанию (см. §Состояния) |

**Условия доступности:**
- «Сбросить» активно, если хотя бы один параметр отличается от дефолта (есть пресет/диапазон дат, или выбран тип конфигурации трассы, или toggle включён, или выбран маршал); иначе — неактивно/скрыто.

### 2. Дата старта

| Элемент | Описание | Источник данных | Валидация | Действие |
|---------|----------|-----------------|-----------|----------|
| Чип «Сегодня» | Пресет: текущий день | `current_filters` | — | Заполнить `date_from`/`date_to` диапазоном текущего дня; снять прочие пресеты |
| Чип «Эта неделя» | Пресет: текущая неделя | `current_filters` | — | Заполнить диапазон недели; снять прочие пресеты |
| Чип «Выходные» | Пресет: ближайшие выходные | `current_filters` | — | Заполнить диапазон выходных; снять прочие пресеты |
| Поле «С» | Начало диапазона (`date_from`) | `current_filters.date_from` | Прошедшие даты недоступны; «С» ≤ «По». Некорректный диапазон не задаётся (правка «С» позже «По» сдвигает «По»). | Открыть date picker |
| Поле «По» | Конец диапазона (`date_to`) | `current_filters.date_to` | «По» ≥ «С». Выбор «По» раньше «С» блокируется/корректируется (показ подсказки) | Открыть date picker |

**Логика:**
- Пресет и ручной диапазон синхронизированы: выбор пресета заполняет поля «С»/«По»; ручная правка любого поля снимает активный пресет. Допустим выбор одной даты (С = По) — фильтр на день.
- **Клиент маппит пресет в `date_from`/`date_to`** (RR-D08): «Сегодня» → диапазон текущего дня; «Эта неделя» → текущая календарная неделя; «Выходные» → ближайшие сб–вс. На сервер уходят только вычисленные `date_from`/`date_to` (отдельного параметра пресета в API нет).
- Применение некорректного диапазона («По» раньше «С») блокируется на уровне контрола (поля не дают его задать) либо подсказкой.

### 3. Тип конфигурации трассы

| Элемент | Описание | Источник данных | Валидация | Действие |
|---------|----------|-----------------|-----------|----------|
| Чип «Новичковая» | Значение `novice` | `current_filters.track_type[]` | — | Тоггл выбора в `track_type[]` |
| Чип «Опытная» | Значение `experienced` | `current_filters.track_type[]` | — | Тоггл выбора в `track_type[]` |

**Логика:**
- Мультивыбор: можно выбрать оба типа одновременно (OR внутри группы). По умолчанию ничего не выбрано (любой тип). Состояние чипа дублируется формой/иконкой, не только цветом.

### 4. Только со свободными местами

| Элемент | Описание | Источник данных | Валидация | Действие |
|---------|----------|-----------------|-----------|----------|
| Toggle «Только со свободными местами» | Параметр `only_available` (`free_seats > 0`) | `current_filters.only_available` | — | Переключить `only_available` true/false |

**Логика:**
- По умолчанию **выключен** (default `false`): в выдаче и заполненные слоты с пометкой «Мест нет». Состояние считывается без опоры на цвет (положение переключателя + текст).

### 5. Маршал

| Элемент | Описание | Источник данных | Валидация | Действие |
|---------|----------|-----------------|-----------|----------|
| Чип маршала | Один чип на маршала | `item.name`, ключ `item.id` из [listMarshals](#listmarshals) | — | Тоггл выбора `item.id` в `marshal_id` |
| Состояние загрузки | Скелетон/индикатор в группе | — | — | — (неблокирующее) |
| Неблокирующая ошибка + «Обновить» | При сбое справочника | — | — | Повторить [listMarshals](#listmarshals) |

**Логика:**
- Мультивыбор: можно отметить нескольких маршалов (OR внутри группы). По умолчанию ничего не выбрано (любой).
- Загрузка справочника неблокирующая: прочие группы доступны сразу; группа маршала показывает скелетон до получения данных. При пустом справочнике (`items` пуст) группа скрывается. При ошибке — неблокирующая ошибка с повтором.

### 6. Нижний CTA

| Элемент | Описание | Источник данных | Валидация | Действие |
|---------|----------|-----------------|-----------|----------|
| Кнопка «Применить» | Фиксированный нижний CTA во всю ширину | Собранные значения фильтров | — | Собрать параметры ([LOGIC-005](09_Логики/LOGIC-005_Фильтрация-слотов.md)) → закрыть шторку → обновить [SCR-002](SCR-002-slot-list.md) запросом [listSlots](#listslots) |

**Логика:**
- Кнопка «Применить»: при тапе собирает `date_from`, `date_to`, `track_type[]`, `marshal_id`, `only_available` из контролов; OR внутри групп мультивыбора, AND между группами ([LOGIC-005](09_Логики/LOGIC-005_Фильтрация-слотов.md)). Закрывает шторку и инициирует обновление SCR-002; на SCR-002 показывается индикатор активных фильтров (если выбор отличается от дефолта).

**Условия доступности:**
- «Применить» активна всегда (в т.ч. при дефолтных значениях — подтверждает выбор/сбрасывает фильтрацию на SCR-002), кроме момента, когда задан некорректный диапазон дат («По» раньше «С») — тогда применение блокируется до исправления.

---

## Состояния экрана

### Таблица состояний

| Состояние | Условие | Отображение |
|-----------|---------|-------------|
| Фильтры по умолчанию | `current_filters` пуст / все в дефолте | Дата — без пресета и диапазона; тип конфигурации трассы — ничего не выбрано; toggle мест — выкл; маршал — ничего не выбрано. «Сбросить» неактивно/скрыто. «Применить» активна. |
| Выбраны фильтры | Хотя бы один параметр ≠ дефолт | Контролы отражают выбор (в т.ч. несколько типов/маршалов); «Сбросить» активно; «Применить» активна. |
| Загрузка справочника маршалов | Ответ `listMarshals` ещё не получен | Группа «Маршал» — скелетон/индикатор; остальные группы интерактивны (неблокирующее). |
| Справочник маршалов пуст | `listMarshals` 200 + `items` пуст | Группа «Маршал» скрыта; прочие фильтры доступны. |
| Ошибка загрузки справочника | `listMarshals` 4xx/5xx/timeout | Группа «Маршал» — неблокирующая ошибка + «Обновить»; прочие фильтры доступны. |
| Применение | Тап «Применить» | Шторка закрывается; результат и empty state — на [SCR-002](SCR-002-slot-list.md). |
| Закрытие без применения | Свайп / тап по бэкдропу / кнопка «✕» | Шторка закрывается; применённые ранее фильтры SCR-002 не меняются (откат изменений в шторке). |

### Диаграмма переходов

```mermaid
stateDiagram-v2
    [*] --> Init : onOpen (предзаполнение из current_filters)

    Init --> InstrLoading : запрос listMarshals

    InstrLoading --> InstrReady : 200 + items
    InstrLoading --> InstrEmpty : 200 + empty (группа скрыта)
    InstrLoading --> InstrError : 4xx/5xx/timeout

    InstrError --> InstrLoading : «Обновить»

    InstrReady --> Editing : выбор/правка значений
    InstrEmpty --> Editing : выбор/правка значений
    InstrError --> Editing : выбор/правка значений (кроме маршала)

    Editing --> Editing : Сбросить (контролы → дефолт)

    Editing --> Applied : «Применить» (диапазон валиден)
    Applied --> [*] : закрыть + обновить SCR-002 (listSlots)

    Editing --> Dismissed : свайп / тап вне / «✕»
    InstrReady --> Dismissed : свайп / тап вне / «✕»
    Dismissed --> [*] : закрыть без применения (фильтры SCR-002 без изменений)
```

---

## Действия пользователя

| Действие | Элемент | Триггер | Результат |
|----------|---------|---------|-----------|
| Выбрать пресет даты | Чип «Сегодня»/«Эта неделя»/«Выходные» | Tap | Заполняется `date_from`/`date_to`; прочие пресеты сняты |
| Задать диапазон вручную | Поля «С» / «По» | Tap → date picker | Устанавливаются `date_from`/`date_to`; активный пресет снимается |
| Выбрать тип конфигурации трассы | Чипы «Новичковая»/«Опытная» | Tap | Тоггл значения в `track_type[]` (мультивыбор, OR) |
| Включить «только со свободными местами» | Toggle | Tap | `only_available` = true/false |
| Выбрать маршала | Чип маршала | Tap | Тоггл `item.id` в `marshal_id` (мультивыбор, OR) |
| Повторить загрузку справочника | «Обновить» в группе маршала | Tap | Повторный [listMarshals](#listmarshals) |
| Сбросить | «Сбросить» в хедере | Tap | Все контролы → дефолт (внутри шторки) |
| Применить | Кнопка «Применить» | Tap | Закрыть шторку + обновить [SCR-002](SCR-002-slot-list.md) ([listSlots](#listslots)) |
| Закрыть без применения | Грабер / бэкдроп / «✕» | Swipe вниз / Tap вне / Tap | Закрыть, фильтры SCR-002 без изменений |

---

## Связанные требования

### Функциональные (REQ-FUNC-*)

| ID | Название | Приоритет |
|----|----------|-----------|
| FR-38 | Фильтрация списка слотов по дате/периоду, типу конфигурации трассы, наличию мест, маршалу | Must |
| FR-9 | Список слотов с составом параметров (контекст применения фильтров) | Must |

### Интеграции (REQ-INT-*)

| ID | Название | Приоритет |
|----|----------|-----------|
| REQ-INT-MARSHAL | `listMarshals` — справочник маршалов для фильтра | Must |
| REQ-INT-SLOTS | `listSlots` — список слотов с query-фильтрами (применение на SCR-002) | Must |

### UI (REQ-UI-*)

| ID | Название | Приоритет |
|----|----------|-----------|
| REQ-UI-BS | Свойства bottom sheet: грабер, swipe-to-close, бэкдроп, кнопка закрытия ([foundations §4.3](../3-design-brief/00-foundations.md)) | High |
| REQ-UI-A11Y | Состояния выбора не только цветом; тач-зоны ≥ 44–48 pt ([foundations §3](../3-design-brief/00-foundations.md)) | High |

### Данные (REQ-DATA-*)

| ID | Название | Приоритет |
|----|----------|-----------|
| REQ-DATA-FILTERS | Передача состояния фильтров между BS-001 и SCR-002 (`current_filters`) | High |

---

## Критерии приёмки

### Позитивные сценарии

| ID | Критерий | Приоритет |
|----|----------|-----------|
| AC-001 | **Дано** клиент на SCR-002, **Когда** он тапает «Фильтры», **Тогда** снизу открывается BS-001 с грабером, заголовком, группами фильтров и кнопкой «Применить»; значения отражают дефолт или ранее применённые фильтры (`current_filters`) | P0 |
| AC-002 | **Дано** открыта шторка, **Когда** клиент отмечает типы «Новичковая» и «Опытная», двух маршалов и тапает «Применить», **Тогда** на SCR-002 остаются слоты, удовлетворяющие ВСЕМ группам (AND), где внутри группы достаточно совпадения с любым из выбранных значений (OR: `track_type=novice` ИЛИ `experienced`, маршал — любой из двух) | P0 |
| AC-003 | **Дано** выбран хотя бы один фильтр, отличный от дефолта, **Когда** клиент тапает «Применить», **Тогда** шторка закрывается, происходит возврат на SCR-002 с обновлённым через `listSlots` списком и виден индикатор активных фильтров | P0 |
| AC-004 | **Дано** включён toggle «Только со свободными местами», **Когда** клиент применяет фильтры, **Тогда** на SCR-002 в `listSlots` передаётся `only_available=true` и отображаются только слоты с `free_seats > 0` | P1 |

### Негативные сценарии

| ID | Критерий | Приоритет |
|----|----------|-----------|
| AC-N01 | **Дано** ошибка сети/сервиса при загрузке справочника, **Когда** открыта шторка, **Тогда** группа «Маршал» показывает неблокирующую ошибку с кнопкой «Обновить», а остальные фильтры остаются доступны и применимы | P1 |
| AC-N02 | **Дано** открыта группа «Дата старта», **Когда** клиент пытается задать «По» раньше «С», **Тогда** некорректный диапазон не применяется (поля не дают его задать либо показывается подсказка), кнопка «Применить» блокируется до исправления | P1 |
| AC-N03 | **Дано** клиент применил фильтры, под которые нет ни одного слота, **Когда** происходит возврат на SCR-002, **Тогда** показывается empty state «Нет слотов по условиям. Попробуйте изменить фильтры.» с действием изменить/сбросить (UC-3 E1) | P0 |

### Граничные условия (Edge Cases)

| ID | Критерий | Приоритет |
|----|----------|-----------|
| AC-E01 | **Дано** на SCR-002 ранее применены фильтры, **Когда** клиент открывает шторку, меняет значения и закрывает её свайпом вниз / тапом по бэкдропу / кнопкой «✕», **Тогда** ранее применённые фильтры на SCR-002 не изменяются (откат изменений шторки) | P0 |
| AC-E02 | **Дано** заданы какие-либо фильтры, **Когда** клиент тапает «Сбросить» и затем «Применить», **Тогда** все параметры возвращаются к дефолту, SCR-002 показывает дефолтную выдачу (слоты на ближайшие 7 дней, `only_available=false`, без прочих фильтров) и индикатор активных фильтров снят | P1 |
| AC-E03 | **Дано** справочник маршалов пуст (`items` пуст), **Когда** шторка инициализирована, **Тогда** группа «Маршал» скрыта, остальные группы фильтров доступны и применимы | P2 |
| AC-E04 | **Дано** выбран только один тип конфигурации трассы, **Когда** клиент применяет фильтр, **Тогда** в `listSlots` передаётся `track_type` с единственным значением, и AND/OR-логика корректно сужает выдачу по этой группе | P2 |


# Ревью дизайна Figma ↔ ТЗ «Апекс»

**Дата:** 2026-06-25 · **Автор:** системный аналитик · **Итерация:** 1
**Источник дизайна:** [Figma «Апекс приложение»](https://www.figma.com/design/ySEt0cjmRqmhdWyDlTpDM5/Апекс-приложение?node-id=71-4857)
(file key `ySEt0cjmRqmhdWyDlTpDM5`, страница «Экраны», стартовый узел `71:4857`)

> Сопоставление фактических макетов с документацией этапов 2 (требования), 4 (модель данных),
> 5 (ТЗ) и OpenAPI. Выгрузка макетов — через Figma REST API (рендер фреймов в PNG, чтение
> визуально). Цель — привести ТЗ/требования/API в соответствие с тем, что реально нарисовано.

---

## 1. Инвентарь экранов: дизайн ↔ ТЗ

| Макет Figma (node-id) | Экран ТЗ | Статус сопоставления |
|------------------------|----------|----------------------|
| Ввод номера / Вход по номеру (71:5377, 71:5401) | SCR-001 шаг 1 | ✅ совпадает |
| Отправка кода / Ввод кода / Отправить код повторно (71:5449, 71:12353, 71:5504) | SCR-001 шаг 2 | ⚠️ OTP **4 ячейки**, таймер 00:30 |
| Регистрация / Ввод имени + ошибка (71:5549, 71:5558) | SCR-001 шаг 3 | ✅ совпадает |
| Загрузка списка / Нет заездов / Ошибка загрузки (71:5695, 71:6002, 71:6026) | SCR-002 (Loading/Empty/Error) | ✅ совпадает (паттерн состояний) |
| Карточка заезда / Booking Sheet (71:6169, 71:14252) | SCR-003 | ⚠️ + hero-изображение, иконка «Поделиться» |
| Карта трассы (71:6176) | BS-004 | ⚠️ + CTA «Проложить маршрут» |
| Оформление записи (71:6145) | SCR-004 | 🔴 лимит мест «до 5» vs «до 4» в ТЗ |
| Запись подтверждена (71:6161) | BS-002 | 🔴 это **полный экран**, в ТЗ — Bottom Sheet |
| Список «Мои записи» (71:6085) | SCR-005 | ✅ вкладки Предстоящие/Прошедшие |
| Детали записи (71:6100) | SCR-006 | ✅ совпадает |
| Отмена записи (71:13106) / Cancellation Sheet (71:3609) | BS-003 | ✅ совпадает (поздняя отмена, штраф не взимается) |
| Фильтры / Filters Container (71:14300) | BS-001 | ⚠️ + пресеты дат «Сегодня / Эта неделя / Выходные» |
| Профиль (71:6677) | SCR-007 | 🔴 нет «Удалить аккаунт»; редактирование **инлайн** (карандаши) |
| Изменение профиля (71:6690) | SCR-007 (edit) | 🔴 инлайн-правка одного поля, не режим «Редактировать всё» |

Компоненты-атомы (Chips, Switch, Filter, Event Card, Equipment, Person, иконки, Tab Bar,
Colors/Main colors) — строительные блоки дизайн-системы, отдельным экранам ТЗ не соответствуют;
учитываются как визуальный язык.

> **Совпадает по навигации:** таб-бар из 3 вкладок (Заезды · Мои записи · Профиль) в дизайне
> присутствует и **уже описан** в ТЗ ([00-foundations §4.2], SCR-002/005/007) — расхождения нет.

---

## 2. Находки (с severity и привязкой к артефактам)

Severity: 🔴 Critical (нужно решение/правка до разработки) · 🟠 High · 🟡 Medium · ⚪ Low.

### 🔴 RR-D01. Лимит мест на бронь: «до 5» в дизайне vs «до 4» в ТЗ
- **Дизайн** (Оформление записи, 71:6145): подпись «Можно записать **до 5 мест**» при слоте со
  «Свободно мест 10 из 14». То есть лимит = `free_seats`, **без** персонального потолка «себя + 3
  гостя».
- **ТЗ/требования:** `max_seats = min(free_seats, track_config.capacity_cap, **4**)` — групповой лимит
  «себя + до 3 гостей» (FR-12, feature-list §2, SCR-004).
- **Конфликт:** дизайн не применяет константу `4`. Либо персональный лимит другой (5 = себя + 4
  гостя), либо его нет вовсе (ограничивают только свободные места и потолок конфигурации трассы).
- **Затрагивает:** FR-12, SCR-004, feature-list §2, slots/bookings (если лимит приходит из API).
- **Требует решения заказчика** (вопрос 1 ниже).

### 🔴 RR-D02. «Удаление аккаунта» отсутствует в макете профиля
- **Дизайн** (Профиль, 71:6677): только Имя, Телефон (инлайн-правка), «Правила центра»,
  «Поддержка», «Версия приложения 1.0.0», кнопка «Выйти». **Кнопки «Удалить аккаунт» нет.**
- **ТЗ/требования/API:** SCR-007 описывает удаление аккаунта (FB-PROFILE-002), есть endpoint
  `deleteAccount` (DELETE `/profile`), последствия по R-009 (аннулирование броней, анонимизация).
- **Конфликт:** функция есть в ТЗ и API, но не нарисована.
- **Требует решения заказчика** (вопрос 2 ниже).

### 🔴 RR-D03. Подтверждение записи — экран, а не Bottom Sheet
- **Дизайн** (Запись подтверждена, 71:6161): **полноэкранный** успех «Вы записаны» со сводкой
  (Мест, Экипировка в прокат, Итого, «Оплата на месте…») и двумя кнопками «Мои бронирования» (primary)
  и «Готово» (secondary).
- **ТЗ:** BS-002 описан как **Bottom Sheet**.
- **Действие:** переклассифицировать BS-002 → экран (например, `SCR-008` или переименовать),
  обновить тип, кнопки и карту навигации (feature-list §3, §4). Запрос push-разрешения
  (LOGIC-007), привязанный к BS-002, остаётся на этом экране успеха.

### 🟠 RR-D04. Редактирование профиля — инлайн по полям, а не режим «Редактировать всё»
- **Дизайн** (Профиль / Изменение профиля): у каждого поля (Имя, Телефон) — иконка-карандаш;
  правка инлайн по одному полю. Кнопки «Редактировать» в хедере и общего «Сохранить» нет.
- **ТЗ:** SCR-007 описывает режим «Редактировать» (хедер) → оба поля вводимы → «Сохранить»/«Отмена».
- **Действие:** привести SCR-007 (макет, элементы, состояния, действия, AC) к инлайн-модели.
  Логика смены телефона через код из SMS сохраняется.

### 🟠 RR-D05. OTP — фиксированные 4 ячейки; таймер повтора 00:30
- **Дизайн:** поле кода — **4 отдельные ячейки** (segmented); таймер «Отправить код повторно
  (00:30)».
- **ТЗ:** длина кода — паттерн `^\d{4,6}$` (не зашивать), таймер — из `resend_after_seconds`
  (пример 60).
- **Действие:** в ТЗ SCR-001/SCR-007 указать UI-паттерн «segmented, число ячеек = длина кода из
  ответа API»; пример таймера привести к 30 с или оставить «из `resend_after_seconds`». Числа
  по-прежнему не хардкодить; уточнить у бэкенда фактическую длину кода (4 или 4–6).

### 🟠 RR-D06. Кнопка «Поделиться» на карточке заезда
- **Дизайн** (Карточка заезда, 71:6169): в хедере иконка «Поделиться» (Share Icon, 70:3394)
  рядом с «Назад».
- **ТЗ:** SCR-003 функцию «поделиться слотом» не описывает.
- **Действие:** решить — специфицировать (share deep-link/текст заезда) или отметить как
  декоративный/Phase 2 (вопрос 3 ниже).

### 🟡 RR-D07. Карточка заезда: hero-изображение и описание конфигурации трассы
- **Дизайн:** вверху карточки/Booking Sheet — фото-обложка; текст «Заезд на трассе
  «Длинная трасса» займёт 15 минут и отлично подойдёт для опытных» (название конфигурации трассы +
  длительность + аудитория).
- **ТЗ/модель/API:** SCR-003 перечисляет параметры, но без обложки и составного описания;
  `TrackConfig` имеет `name`, `duration_min`, но нет явного поля описания/изображения.
- **Действие:** добавить в SCR-003 элементы «обложка» и «описание конфигурации трассы»; в API оценить поля
  `TrackConfig.description` (или составной текст на клиенте) и `Slot/TrackConfig` image URL. Если изображение
  не из MVP — отметить как декоративный плейсхолдер.

### 🟡 RR-D08. Фильтры: пресеты дат «Сегодня / Эта неделя / Выходные»
- **Дизайн** (Фильтры, 71:14300): быстрые чипы пресетов дат + кастомный диапазон «с / по»; тип
  конфигурации трассы (Новичковая/Опытная); маршал (Анна/Игорь/Наталья из справочника); тумблер «Только
  со свободными местами»; «Применить» / «Сбросить».
- **ТЗ:** BS-001 описывает фильтры по дате/периоду, конфигурации трассы, маршалу, «только свободные», но без
  явных пресетов.
- **Действие:** добавить в BS-001 пресеты дат (клиент маппит в `date_from`/`date_to`); проверить,
  что параметры фильтра слотов в API это покрывают.

### 🟡 RR-D09. Карта трассы: CTA «Проложить маршрут»
- **Дизайн** (Карта трассы, 71:6176): кнопки «Проложить маршрут» (primary) и «Открыть в
  Яндекс.Картах» (secondary), линия трассы + метка «Картинг-центр «Апекс», въезд 1», текст «Заезд на трассе займёт
  15 минут».
- **ТЗ:** BS-004 описывает интерактивную карту и место сбора; «Проложить маршрут» (построение
  маршрута до точки сбора) не выделен отдельной кнопкой.
- **Действие:** добавить CTA «Проложить маршрут» в BS-004 (deep-link навигации к точке сбора).

### ⚪ RR-D10. Терминология заголовков
- **Дизайн:** заголовки/табы — «Заезды», «Мои записи», «Профиль»; экран успеха «Вы записаны».
- **ТЗ:** «Список слотов» (SCR-002), «Мои бронирования» (SCR-005).
- **Действие:** синхронизировать видимые подписи в ТЗ с дизайном (микрокопия), сохранив
  ID-экранов. Внутренние названия ТЗ можно оставить, но в макетах/копирайте указать фактические.

### ⚪ RR-D11. Микрокопия пустых/ошибочных состояний
- **Дизайн:** Empty «Пока нет доступных заездов / Загляните позже»; Error «Не удалось загрузить
  / Проверьте соединение и попробуйте снова / Обновить»; снек «Не удалось загрузить данные.
  Проверьте интернет».
- **Действие:** сверить и при необходимости обновить каталог формулировок в
  [00-foundations §6] и состояниях SCR-002.

---

## 3. Влияние на документы (план правок)

| Находка | ТЗ (5-mobile-app-spec) | Требования (2-requirements) | OpenAPI (api/) |
|---------|------------------------|------------------------------|----------------|
| RR-D01 | SCR-004, feature-list §2 | FR-12 (лимит мест) | slots/bookings — если лимит из API |
| RR-D02 | SCR-007, feature-list §4 | FR/NFR удаления, R-009 | возможно пометить `deleteAccount` (если вне MVP) |
| RR-D03 | BS-002 → экран, feature-list §3/§4 | — | — |
| RR-D04 | SCR-007 (макет/элементы/AC) | — | — |
| RR-D05 | SCR-001, SCR-007 | — | auth/profile: длина `code` |
| RR-D06 | SCR-003 | US (share), при включении | — |
| RR-D07 | SCR-003, модель данных | — | `TrackConfig.description`/image |
| RR-D08 | BS-001 | FR-38 (пресеты) | slots: `date_from/date_to` |
| RR-D09 | BS-004 | — | — |
| RR-D10/11 | SCR-002/005, foundations | — | — |

---

## 4. Решения заказчика (согласовано 2026-06-25)

1. **Лимит мест на одну бронь** (RR-D01): **остаётся 4** («себя + 3 гостя»),
   `max_seats = min(free_seats, track_config.capacity_cap, 4)`. Подпись на макете «Можно записать до
   5 мест» — **неточность дизайна**; ТЗ не меняем, передать дизайнеру правку макета (показывать
   «до 4», т.е. динамически `min(free_seats, cap, 4)`).
2. **Удаление аккаунта** (RR-D02): **остаётся в MVP**. На макете профиля кнопки «Удалить
   аккаунт» нет — это **пробел дизайна**; ТЗ/API сохраняем, передать дизайнеру добавление кнопки
   на экран профиля (ниже «Выйти», деструктивная, менее акцентная).
3. **«Поделиться» заездом** (RR-D06): **Phase 2 / декор**. В MVP не специфицируем; в ТЗ
   SCR-003 иконку отметить как неактивную/отложенную, добавить строку в feature-list §7.

Остальные находки (RR-D03/04/05/07/08/09/10/11) — приведение ТЗ к фактическому дизайну, решения
заказчика не требуют; вносятся правками.

> **Флажки дизайнеру (правки макета, вне scope ТЗ):** (а) лимит мест на «Оформление записи» →
> «до 4»; (б) добавить «Удалить аккаунт» на экран «Профиль».

---

## 5. Метод выгрузки (воспроизводимость)

- Скрипт: `GET /v1/files/{key}?depth=4` → сбор фреймов-экранов → `GET /v1/images` (PNG, scale 2)
  → скачивание + индекс. Токен — из `.env` (`FIGMA_TOKEN`, не коммитится).
- Выгружено 57 фреймов (22 экрана + компоненты/цвета). PNG хранятся вне репозитория (скретчпад).
